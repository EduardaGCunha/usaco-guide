#include <bits/stdc++.h>
using namespace std;

int main(){
    int n, m; cin >> n >> m;
    map<int, int> mp;
    int arr[n+1];
    for(int i = 0; i < n; i++) cin >> arr[i];
    for(int i = 0; i < m; i++){
        int b; cin >> b;
        mp[b]++;
    }
   
}