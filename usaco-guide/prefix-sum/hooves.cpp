#include <bits/stdc++.h>
using namespace std;

int main(){
    int n; cin >> n;
    vector<vector<int>> prefixSum(n, vector<int>(n, 0));
    for(int i = 0; i < n; i++){
        char a; cin >> a;
        
    }


}