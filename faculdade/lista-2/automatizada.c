#include <stdio.h>

int main(){
    int a, b, c, d, e, f, g, h, i, j;
    scanf("%d %d %d %d %d %d %d %d %d %d", &a, &b, &c, &d, &e, &f, &g, &h, &i, &j);
    if(a == f){
        printf("N\n");
        return 0;
    }else if(c == h){
        printf("N\n");
        return 0;
    }else if(d == i){
        printf("N\n");
        return 0;
    }else if(e == j){
        printf("N\n");
        return 0;
    }else if(b == g){
        printf("N\n");
        return 0;
    }

    printf("Y\n");
}