#include <bits/stdc++.h>
using namespace std;

const int MAXN = 1e3;
pair<int, int> coord[MAXN];

int main(){
    int n; cin >> n;
    for(int i = 0; i < n; i++){
        int x, y; cin >> x >> y;
        coord[i].first = x;
        coord[i].second = y;
    }

    int l = 0, r = 1e6;
    while(l < r){
        int mid = (l+r)/2;
        
    } 
}