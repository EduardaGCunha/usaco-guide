#include <bits/stdc++.h>
using namespace std;

int main(){
    int n, m, x, y; cin >> n >> m >> x >> y;
    if(x > n && y > m){
        cout << "O JOGO DUROU " << x - n << " HORA(S) E " << m - y << " MINUTO(S)";
    }else if(x==n && y == n){
        
    }
}