#include <bits/stdc++.h>
using namespace std;

#define int long long
const int MAXN = 50;
int n, m; 
int dp[MAXN][MAXN];

int pd(int i, int j){
    if(i == n-1 && j == m-1) return 0;
    if(i >= n || j >= m) return INT_MIN;
    
}

int main(){

}